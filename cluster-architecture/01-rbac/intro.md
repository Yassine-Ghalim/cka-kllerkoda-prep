# Cluster Arch: RBAC for a ServiceAccount

Grant a ServiceAccount read-only access to pods in a namespace — nothing more.

**Domain:** Cluster Architecture, Installation & Configuration (25%)

> Works on any cluster.

The environment is being prepared — wait for the prompt, then read the Task.
