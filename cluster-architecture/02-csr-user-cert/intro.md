# Cluster Arch: create & approve a user certificate (CSR)

Onboard user 'jane' via a CertificateSigningRequest and bind RBAC.

**Domain:** Cluster Architecture, Installation & Configuration (25%)

> Works on any cluster with openssl available.

The environment is being prepared — wait for the prompt, then read the Task.
