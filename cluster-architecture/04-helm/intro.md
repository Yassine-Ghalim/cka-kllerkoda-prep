# Cluster Arch: install an app with Helm

Install Helm and deploy a release with a custom value.

**Domain:** Cluster Architecture, Installation & Configuration (25%)

> Helm install needs internet once (to fetch the binary). Offline-friendly otherwise (uses 'helm create').

The environment is being prepared — wait for the prompt, then read the Task.
