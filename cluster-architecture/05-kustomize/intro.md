# Cluster Arch: customise a base with Kustomize

Write an overlay that changes namespace, prefix, replicas and image, then apply.

**Domain:** Cluster Architecture, Installation & Configuration (25%)

> Works on any cluster (kustomize is built into kubectl). Only kubernetes.io/docs Kustomize page is allowed in the exam.

The environment is being prepared — wait for the prompt, then read the Task.
