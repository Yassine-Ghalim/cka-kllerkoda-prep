# Cluster Arch (bonus): back up etcd

Take an etcdctl snapshot of the cluster datastore.

**Domain:** Cluster Architecture, Installation & Configuration (25%)

> Deprioritised in the current curriculum but classic. Requires controlplane access + etcdctl + pki certs (kubeadm).

The environment is being prepared — wait for the prompt, then read the Task.
