# Services & Networking: cluster DNS is broken

Service discovery fails cluster-wide. Restore CoreDNS.

**Domain:** Services & Networking (20%)

> Works on any cluster running CoreDNS as a Deployment in kube-system.

The environment is being prepared — wait for the prompt, then read the Task.
