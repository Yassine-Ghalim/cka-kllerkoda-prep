# Services & Networking: route HTTP with an Ingress

Create host/path routing rules to two backend Services.

**Domain:** Services & Networking (20%)

> The Ingress object is created regardless; serving real traffic needs an ingress controller installed.

The environment is being prepared — wait for the prompt, then read the Task.
