# Services & Networking (advanced): Gateway API

Install Gateway API CRDs and route with a Gateway + HTTPRoute.

**Domain:** Services & Networking (20%)

> Needs internet once to fetch CRDs. Routing traffic needs a Gateway controller; verify checks the objects are well-formed.

The environment is being prepared — wait for the prompt, then read the Task.
