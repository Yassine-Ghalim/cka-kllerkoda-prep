# Troubleshooting: kube-apiserver won't start

The control plane API server is down after a bad config change. Diagnose and fix it.

**Domain:** Troubleshooting (30%)

> Requires a kubeadm cluster (static pods + crictl). Works on Killercoda controlplane and local kubeadm nodes. Not for minikube/kind.

The environment is being prepared — wait for the prompt, then read the Task.
