# Troubleshooting: worker node NotReady

node01 went NotReady. Find the cause on the node and bring it back.

**Domain:** Troubleshooting (30%)

> Requires systemctl access to the node (kubeadm-style). On Killercoda the controlplane can 'ssh node01'. On a local cluster, run the fix on the affected node.

The environment is being prepared — wait for the prompt, then read the Task.
