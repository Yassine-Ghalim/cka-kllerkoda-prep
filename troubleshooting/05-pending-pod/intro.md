# Troubleshooting: Pod stuck Pending (scheduling)

A pod can't be scheduled. Diagnose the constraint and get it Running.

**Domain:** Troubleshooting (30%)

> Works on any multi-node cluster (uses a nodeSelector that matches nothing).

The environment is being prepared — wait for the prompt, then read the Task.
